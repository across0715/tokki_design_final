<div>
  <%= form.label :name %> (optional))
  <%= form.text_field :name %>
</div>
<div>
  <%= form.label :email %> (required)
  <%= form.email_field :email, required: true, pattern: "^[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]+$" %>
</div>
<div>
  <%= form.label :content %> (required)
  <%= form.text_area :content, required: true %>
</div>
